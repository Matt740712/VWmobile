name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.10+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.13.0 # Aggiungi il pacchetto per le icone
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/back_ground_verywell.png
    - assets/images/back_ground.jpg

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/back_ground_verywell.png" # Percorso della tua icona
